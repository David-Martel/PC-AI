[build]
incremental = true

[net]
git-fetch-with-cli = true
retry = 3

[registries.crates-io]
protocol = "sparse"

[alias]
pcai-check = "check --workspace --all-targets"
pcai-clippy = "clippy --workspace --all-targets -- -D warnings"
pcai-fmt-check = "fmt --all -- --check"
