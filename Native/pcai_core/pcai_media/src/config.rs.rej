diff a/Native/pcai_core/pcai_media/src/config.rs b/Native/pcai_core/pcai_media/src/config.rs	(rejected hunks)
@@ -269,6 +269,49 @@ mod tests {
         assert!(cfg.resolve_device().is_err());
     }
 
+    /// `resolve_device` on `"cuda"` or `"cuda:0"` must attempt to load CUDA device 0.
+    #[test]
+    #[cfg(feature = "cuda")]
+    fn test_resolve_device_cuda_default() {
+        for dev_str in ["cuda", "cuda:0", " CUDA ", "cUDa:0"] {
+            let cfg = PipelineConfig {
+                device: dev_str.to_string(),
+                ..PipelineConfig::default()
+            };
+            let dev = cfg.resolve_device().unwrap_or_else(|e| {
+                panic!("failed to resolve device '{dev_str}': {e}");
+            });
+            assert!(matches!(dev, Device::Cuda(_)));
+        }
+    }
+
+    /// `resolve_device` on `"cuda:1"` must attempt to load CUDA device 1.
+    #[test]
+    #[cfg(feature = "cuda")]
+    fn test_resolve_device_cuda_ordinal() {
+        let cfg = PipelineConfig {
+            device: "cuda:1".to_string(),
+            ..PipelineConfig::default()
+        };
+        // Depending on the test machine, device 1 might not exist.
+        // We just ensure it parses and attempts creation.
+        match cfg.resolve_device() {
+            Ok(Device::Cuda(_)) => {}
+            Err(e) if e.to_string().contains("failed to open CUDA device 1") => {}
+            other => panic!("expected successful parse and Device::Cuda or expected error, got {other:?}"),
+        }
+    }
+
+    /// Invalid CUDA ordinals must return an error.
+    #[test]
+    fn test_resolve_device_cuda_invalid_ordinal() {
+        let cfg = PipelineConfig {
+            device: "cuda:auto".to_string(),
+            ..PipelineConfig::default()
+        };
+        assert!(cfg.resolve_device().is_err());
+    }
+
     /// `resolve_dtype` must map recognised strings correctly.
     #[test]
     fn test_resolve_dtype_variants() {
